// @generated by migration/tools/gen_help_text.py from hoocode cli/args.ts
// printHelp() at the pin. Do not edit by hand. Branding: hoocode -> cortex.

use crate::help::Seg;

pub(crate) const HELP_SEGMENTS: &[Seg] = &[
    Seg::Bold("cortex"),
    Seg::Plain(" - AI coding assistant with read, bash, edit, write tools\n\n"),
    Seg::Bold("Usage:"),
    Seg::Plain("\n  cortex [options] [@files...] [messages...]\n\n"),
    Seg::Bold("Commands:"),
    Seg::Plain("\n  cortex install <source> [-l]     Install extension source and add to settings\n  cortex remove <source> [-l]      Remove extension source from settings\n  cortex uninstall <source> [-l]   Alias for remove\n  cortex update [source|self]      Update cortex and installed extensions\n  cortex list                      List installed extensions from settings\n  cortex config                    Open TUI to enable/disable package resources\n  cortex resources                 List discovered skills, subagents, slash commands, and MCP servers\n  cortex <command> --help          Show help for install/remove/uninstall/update/list\n\n"),
    Seg::Bold("Options:"),
    Seg::Plain("\n  --provider <name>              Provider name (default: google)\n  --model <pattern>              Model pattern or ID (supports \"provider/id\" and optional \":<thinking>\")\n  --api-key <key>                API key (defaults to env vars)\n  --system-prompt <text>         System prompt (default: coding assistant prompt)\n  --mode <mode>                  Output mode: text (default), json, or rpc\n  --print, -p                    Non-interactive mode: process prompt and exit\n  --continue, -c                 Continue previous session\n  --resume, -r                   Select a session to resume\n  --session <path|id>            Use specific session file or partial UUID\n  --fork <path|id>               Fork specific session file or partial UUID into a new session\n  --session-dir <dir>            Directory for session storage and lookup\n  --no-session                   Don't save session (ephemeral)\n  --team <url|auto>              Bridge a hooteams server into the task panel's teams view\n                                 (focus roles, nudge, attach, answer approval gates);\n                                 \"auto\" finds .agents/teams/default.json\n                                 or hooteams.config.json upward from cwd and spawns hooteams locally\n  --models <patterns>            Comma-separated model patterns for Ctrl+P cycling\n                                 Supports globs (anthropic/*, *sonnet*) and fuzzy matching\n  --no-tools, -nt                Disable all tools by default (built-in and extension)\n  --no-builtin-tools, -nbt       Disable built-in tools by default but keep extension/custom tools enabled\n  --tools, -t <tools>            Comma-separated allowlist of tool names to enable\n                                 Applies to built-in, extension, and custom tools\n  --disallowed-tools <tools>     Comma-separated denylist of tool names to disable\n                                 (subtracted from the allowlist or default set)\n  --max-turns <n>                Hard cap on assistant turns; the agent is asked to wrap up near the\n                                 cap and stopped at it (mainly used for spawned subagents)\n  --enable-subagents             Enable the subagent tool (delegate tasks to isolated agent loops).\n                                 On by default (the \"enableSubagent\" setting defaults to true)\n  --no-subagents                 Disable the subagent tool for this session (overrides the setting)\n  --warm-subagents               Dispatch eligible subagents on reused warm RPC workers (experimental)\n  --max-subagent-depth <n>       Tree-wide subagent nesting cap (default 2 = one level of nesting;\n                                 1 = no nesting). Overrides the \"maxSubagentDepth\" setting\n                                 Can also be enabled via the \"enableSubagent\" setting\n  --enable-todowrite             Enable the TodoWrite tool (maintain a live todo list in the task panel)\n                                  Can also be enabled via the \"enableTodoWrite\" setting\n  --enable-webtools              Enable the webfetch + websearch tools (network access, off by default)\n                                  Can also be enabled via the \"enableWebTools\" setting\n                                  Block hosts with a .webtoolsignore file (gitignore syntax)\n  --enable-semantic-index        Enable the semantic index layer (on by default)\n                                  The unified SearchCodebase tool (lexical, semantic, hybrid\n                                  rank-fused modes) is always available and cannot be turned\n                                  off; this flag only controls whether the semantic index is\n                                  built and fused in. The semantic layer is ON by\n                                  default: on session start every repo is indexed with the\n                                  embsearch binary (local MiniLM embeddings, stored under\n                                  ~/.cortexcode/embsearch), regardless of size; until the index is\n                                  ready search runs lexical-only, then fuses semantic hits once\n                                  ready. Requires embsearch on PATH or\n                                  the \"embsearchBinaryPath\" setting; degrades to lexical-only if\n                                  the binary is unavailable. Disable via the \"enableSemanticIndex\"\n                                  setting (false), or raise \"embsearchThresholdBytes\" to skip\n                                  small repos.\n  --enable-plugintools           Enable the autonomous plugin system (off by default)\n                                  Plugin lifecycle tools (SearchPlugins, InstallPlugin, ...) and\n                                  ProposePlugin, plus the runtime plugin-reuse nudge\n                                  Can also be enabled via the \"enablePluginTools\" setting,\n                                  which /settings -> Plugins -> Plugin system writes\n  --light                        Minimal low-token preset for small/local models:\n                                  only the read/write/edit/bash tools (short descriptions,\n                                  stripped schemas; search via bash), a terse system prompt, and\n                                  no subagents/TodoWrite/skills/context files/mode appendix\n                                  Can also be enabled via the \"light\" setting\n  --print-token-surface          Print the fixed per-turn surface (system prompt + serialized\n                                  tool schema token estimate) and exit\n  --platform <list>              Platform layout(s) cortex targets when it writes artifacts:\n                                  authored plugins (ProposePlugin) and the /new-skill /new-agent\n                                  /new-command scaffolds. Comma-separated and/or repeated.\n                                  Tokens: claude, copilot (alias: github, gh), agents (alias: native)\n                                  e.g. --platform copilot writes .github/skills/<name>/SKILL.md,\n                                  .github/agents/<name>.agent.md, .github/prompts/<name>.prompt.md and\n                                  a .github/plugin/plugin.json manifest for authored plugins\n                                  Plugins are only ever produced for claude or github: they are\n                                  distribution units and the native agents layout belongs to no\n                                  marketplace, so agents is ignored for them (scaffolds still use it)\n                                  Default: claude for plugins; .cortexcode/ for scaffolds\n                                  Can also be set via the \"platform\" setting, which\n                                  /settings -> Plugins -> Platform writes (per environment)\n  --thinking <level>             Set thinking level: off, minimal, low, medium, high, xhigh\n  --extension, -e <path>         Load an extension file (can be used multiple times)\n  --no-extensions, -ne           Disable extension discovery (explicit -e paths still work)\n  --skill <path>                 Load a skill file or directory (can be used multiple times)\n  --no-skills, -ns               Disable skills discovery and loading\n  --agent <path>                 Load an agent file or directory (can be used multiple times)\n  --prompt-template <path>       Load a prompt template / slash command file or directory (repeatable)\n  --slash-command <path>         Alias of --prompt-template (prompts and slash commands are one feature)\n  --no-prompt-templates, -np     Disable prompt template / slash command discovery (same as --no-slash-commands)\n  --no-slash-commands, -nsc      Disable prompt template / slash command discovery (same as --no-prompt-templates)\n  --theme <path>                 Load a theme file or directory (can be used multiple times)\n  --no-themes                    Disable theme discovery and loading\n  --mode-path <dir>              Add a directory to search for {name}/system.md mode files (can be used multiple times)\n  --no-context-files, -nc        Disable AGENTS.md and CLAUDE.md discovery and loading\n  --export <file>                Export session file to HTML and exit\n  --list-models [search]         List available models (with optional fuzzy search)\n  --verbose                      Force verbose startup (overrides quietStartup setting)\n  --offline                      Disable startup network operations (same as CORTEX_OFFLINE=1)\n  --ca-cert <path>               Trust an extra PEM CA bundle for cortex's own TLS traffic (provider\n                                 calls, GitHub API, downloads). Additive to the bundled roots;\n                                 verification stays ON. Also: CORTEX_CA_CERT / NODE_EXTRA_CA_CERTS.\n                                 Does not affect the webfetch/websearch binary.\n  --use-system-ca                Also trust the OS/system CA store (opt-in). Same as\n                                 CORTEX_USE_SYSTEM_CA=1\n  --help, -h                     Show this help\n  --version, -v                  Show version number\n\nExtensions can register additional flags (e.g., --plan from plan-mode extension)."),
    Seg::Ext,
    Seg::Plain("\n\n"),
    Seg::Bold("Examples:"),
    Seg::Plain("\n  # Interactive mode\n  cortex\n\n  # Interactive mode with initial prompt\n  cortex \"List all .ts files in src/\"\n\n  # Include files in initial message\n  cortex @prompt.md @image.png \"What color is the sky?\"\n\n  # Non-interactive mode (process and exit)\n  cortex -p \"List all .ts files in src/\"\n\n  # Multiple messages (interactive)\n  cortex \"Read package.json\" \"What dependencies do we have?\"\n\n  # Continue previous session\n  cortex --continue \"What did we discuss?\"\n\n  # Use different model\n  cortex --provider openai --model gpt-4o-mini \"Help me refactor this code\"\n\n  # Use model with provider prefix (no --provider needed)\n  cortex --model openai/gpt-4o \"Help me refactor this code\"\n\n  # Use model with thinking level shorthand\n  cortex --model sonnet:high \"Solve this complex problem\"\n\n  # Limit model cycling to specific models\n  cortex --models claude-sonnet,claude-haiku,gpt-4o\n\n  # Limit to a specific provider with glob pattern\n  cortex --models \"github-copilot/*\"\n\n  # Cycle models with fixed thinking levels\n  cortex --models sonnet:high,haiku:low\n\n  # Start with a specific thinking level\n  cortex --thinking high \"Solve this complex problem\"\n\n  # Read-only mode (no file modifications possible)\n  cortex --tools read,SearchCodebase -p \"Review the code in src/\"\n\n  # Export a session file to HTML\n  cortex --export ~/.cortexcode/agent/sessions/--path--/session.jsonl\n  cortex --export session.jsonl output.html\n\n"),
    Seg::Bold("Environment Variables:"),
    Seg::Plain("\n  ANTHROPIC_API_KEY                - Anthropic Claude API key\n  ANTHROPIC_OAUTH_TOKEN            - Anthropic OAuth token (alternative to API key)\n  OPENAI_API_KEY                   - OpenAI GPT API key\n  AZURE_OPENAI_API_KEY             - Azure OpenAI API key\n  AZURE_OPENAI_BASE_URL            - Azure OpenAI/Cognitive Services base URL (e.g. https://{resource}.openai.azure.com)\n  AZURE_OPENAI_RESOURCE_NAME       - Azure OpenAI resource name (alternative to base URL)\n  AZURE_OPENAI_API_VERSION         - Azure OpenAI API version (default: v1)\n  AZURE_OPENAI_DEPLOYMENT_NAME_MAP - Azure OpenAI model=deployment map (comma-separated)\n  DEEPSEEK_API_KEY                 - DeepSeek API key\n  GEMINI_API_KEY                   - Google Gemini API key\n  GOOGLE_CLOUD_PROJECT             - Cloud project for Code Assist logins (paid/Workspace accounts)\n  CORTEX_ANTIGRAVITY_CLIENT_ID    - OAuth client id for the Google Antigravity login\n  CORTEX_ANTIGRAVITY_CLIENT_SECRET - OAuth client secret for the Google Antigravity login\n  CORTEX_GEMINI_CLI_CLIENT_ID     - OAuth client id for the Gemini CLI login\n  CORTEX_GEMINI_CLI_CLIENT_SECRET - OAuth client secret for the Gemini CLI login\n  GROQ_API_KEY                     - Groq API key\n  CEREBRAS_API_KEY                 - Cerebras API key\n  XAI_API_KEY                      - xAI Grok API key\n  FIREWORKS_API_KEY                - Fireworks API key\n  TOGETHER_API_KEY                 - Together AI API key\n  OPENROUTER_API_KEY               - OpenRouter API key\n  AI_GATEWAY_API_KEY               - Vercel AI Gateway API key\n  ZAI_API_KEY                      - ZAI API key\n  MINIMAX_API_KEY                  - MiniMax API key\n  MOONSHOT_API_KEY                 - Moonshot AI API key\n  OPENCODE_API_KEY                 - OpenCode Zen/OpenCode Go API key\n  KIMI_API_KEY                     - Kimi For Coding API key\n  XIAOMI_API_KEY                   - Xiaomi MiMo API key (api.xiaomimimo.com billing)\n  XIAOMI_TOKEN_PLAN_CN_API_KEY     - Xiaomi MiMo Token Plan API key (China region)\n  XIAOMI_TOKEN_PLAN_AMS_API_KEY    - Xiaomi MiMo Token Plan API key (Amsterdam region)\n  XIAOMI_TOKEN_PLAN_SGP_API_KEY    - Xiaomi MiMo Token Plan API key (Singapore region)\n  NVIDIA_API_KEY                   - NVIDIA API key (integrate.api.nvidia.com)\n  CORTEX_CODING_AGENT_DIR          - Config directory (default: ~/.cortexcode/agent)\n  CORTEX_CODING_AGENT_SESSION_DIR  - Session storage directory (overridden by --session-dir)\n  CORTEX_PACKAGE_DIR              - Override package directory (for Nix/Guix store paths).\n  CORTEX_OFFLINE                  - Disable startup network operations when set to 1/true/yes.\n  CORTEX_NATIVE_SEARCH            - Force the pure-JS search fallback instead of the fd/rg binaries\n                                     when set to 1 (also used automatically when fd/rg are unavailable).\n  CORTEX_CA_CERT                  - Path to an extra PEM CA bundle to trust for cortex's own TLS traffic\n                                     (additive; verification stays on). Precedence: --ca-cert > this > NODE_EXTRA_CA_CERTS\n  CORTEX_USE_SYSTEM_CA            - Also trust the OS/system CA store when set to 1/true/yes (same as --use-system-ca)\n  NODE_EXTRA_CA_CERTS              - Standard Node path to an extra PEM CA bundle (used as the lowest-precedence CA source)\n  CORTEX_WEBTOOLS_CA_CERT         - Path to a PEM CA bundle forwarded to the webtools binary (webfetch/websearch) as --ca-cert\n  CORTEX_WEBTOOLS_INSECURE        - Forward --insecure to the webtools binary (disables its TLS verification) when set to 1/true/yes\n  CORTEX_TELEMETRY                - Override install telemetry when set to 1/true/yes or 0/false/no.\n  CORTEX_SHARE_VIEWER_URL         - Base URL of a session viewer for /share. Unset by default;\n                                     /share then reports the gist URL alone.\n\n"),
    Seg::Bold("Built-in Tool Names:"),
    Seg::Plain("\n  read   - Read file contents\n  bash   - Execute bash commands\n  edit   - Edit files with find/replace\n  write  - Write files (creates/overwrites)\n  search - Ranked code search, keyword + semantic (read-only)\n"),
];
