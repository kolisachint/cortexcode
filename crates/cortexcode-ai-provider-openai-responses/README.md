# cortexcode-ai-provider-openai-responses

OpenAI Responses API (`openai-responses`) provider for cortex AI, ported from hoocode
`providers/openai-responses.ts`. The `shared` module (message/tool conversion, the
stream-event processor and the HTTP driver, from `openai-responses-shared.ts`) is also
used by `cortexcode-ai-provider-azure`.
