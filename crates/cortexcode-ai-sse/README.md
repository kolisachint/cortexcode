# cortexcode-ai-sse

Server-Sent Events decoder shared by the cortex AI providers. The only crate allowed to depend
on `eventsource-stream` (see `migration/dep-firewall.json`).

Part of the [cortexcode](https://github.com/kolisachint/cortexcode) Rust workspace.
