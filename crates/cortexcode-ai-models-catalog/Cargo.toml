[package]
name = "cortexcode-ai-models-catalog"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Model catalog data (LLM and image models) for cortex AI, generated from the pinned hoocode"
readme = "README.md"

[package.metadata.cortex]
publish = false

[dependencies]
